---
title: Test Code Formatting Post
slug: test-code-formatting-post
---

Test JavaScript fenced codeblock

```js
const blah = 'blah';
const blarb = (blep) => {
  if(blep === 1) {
    return blep + 1;
  }
}
```